export enum TransactionState {
    REQUESTED = "REQUESTED",
    CANCELLED = "CANCELLED", //buna bakacaz.
    COMPLATED = "COMPLATED",
}

export enum TransactionType {
    WITHDRAW = "WITHDRAW",
    VIRMAN = "VIRMAN",
    DEPOSIT = "DEPOSIT"
}